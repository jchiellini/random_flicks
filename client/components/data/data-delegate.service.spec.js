'use strict';

describe('Service: dataDelegate', function () {

  // load the service's module
  beforeEach(module('randomFlicksApp'));

  // instantiate service
  var dataDelegate;
  beforeEach(inject(function (_dataDelegate_) {
    dataDelegate = _dataDelegate_;
  }));

//  it('should do something', function () {
//    expect(!!dataDelegate).toBe(true);
//  });

});
